install.packages("tidyverse",dep=TRUE)  #instalando paquetes

library(tidyverse) #importar la libreria

install.packages("magic",dep = TRUE)

library(magic)

magic(6)
magic(10)

installed.packages() #verificar los paquetes instalados en R
